import { defineConfig } from 'astro/config';

// https://docs.astro.build/en/reference/configuration-reference/
export default defineConfig({
  // Si vas a desplegar en GitHub Pages como proyecto (no en un dominio propio
  // ni en user.github.io raíz), descomentá estas dos líneas y reemplazá los valores:
  //
  // site: 'https://TU-USUARIO.github.io',
  // base: '/finanzas-dashboard',
});
